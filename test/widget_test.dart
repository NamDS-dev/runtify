import 'package:flutter_test/flutter_test.dart';

void main() {
  // TODO: Firebase 초기화가 필요해서 기본 테스트는 비워둠
  // Firebase Mock 설정 후 테스트 추가 예정
  testWidgets('Runtify app placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
