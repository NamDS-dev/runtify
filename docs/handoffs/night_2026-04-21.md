---
title: 야간 PM 활동 요약 (2026-04-20 밤 → 2026-04-21 아침)
mode: /pm-night (본격 세션)
session_window: 2026-04-20 23:53 ~ 2026-04-21 00:05 (약 12분)
---

## 🌙 야간 PM 활동 요약

### 📊 오딧 결과
- 영역: **인증/보안** (FEATURE_PLAN.md에 기록된 🟢 3건을 본 세션에서 전량 소진)
- 발견된 신규 갭: 0건 (기존 오딧 결과 재사용)
- 현재 인증/보안 잔여 갭: 🟡 4건 / 🔴 1건 (모두 사용자 결정 필요)

### ✅ 자동 구현 완료 (3개)
1. `d2fd4e8` **이메일 형식 검증 및 정규화 일관 적용**
   - `lib/core/validators/email_validator.dart` 신설 (RFC 5322 정규식 + 길이 제한 + normalize)
   - 로그인/회원가입 폼 validator 교체, 데이터소스에서도 방어적 정규화
   - Firebase Auth/Firestore 저장 값까지 일관 정규화 → 대소문자/공백 차이로 인한 로그인 실패 방지
   - 테스트 6건 추가 (빈 값/형식 오류/정상/과도한 길이/정규화 결과)

2. `6eac328` **비밀번호 복잡도 규칙 적용 + 실시간 강도 표시**
   - `lib/core/validators/password_validator.dart` 신설
   - 가입: 8자+ / 대·소·숫자 각 1자 이상 / 흔한 비번 16종 차단 (OWASP 기반)
   - 로그인: 기존 6자+ 유지 (기존 사용자 호환)
   - `PasswordStrengthBar` 위젯 — 가입 모드에서 4단 실시간 강도 표시
   - 테스트 11건 추가

3. `a685a83` **로그아웃 시 사용자 범위 SharedPreferences 클리어**
   - BLE 페어링(`ble_device_id/name/onboarding_done`) + Health Connect 온보딩 키 삭제
   - 공유 기기에서 이전 사용자 장치/설정이 다음 로그인에 노출되는 문제 차단
   - 테마 선호도(`runtify_theme_mode`)는 기기 설정이라 유지
   - Google/Firebase/SharedPreferences 3단계를 try-catch 격리 → 부분 실패 시 나머지 cleanup 보장

세션 총 로컬 커밋 4건 (구현 3 + 문서 `c4173a1` 1). git push 없음.

### 🟡 사용자 검토 필요 (잔여, FEATURE_PLAN.md 참조)
- **[인증] 비밀번호 재설정 기능** — 재설정 이메일 템플릿 기본 vs 커스텀 결정 필요
- **[인증] 이메일 인증(Verification) 플로우** — 어느 기능까지 인증 전 허용할지 정책 결정 필요
- **[인증] 연속 로그인 실패 레이트 리밋** — Blaze 요금제 전환 여부 결정 필요
- **[인증] 세션 만료 및 토큰 갱신 처리** — 유휴 시간 정책 결정 필요

### 🔴 정책/보안 결정 필요 (잔여)
- **[인증] 계정 탈퇴(Account Deletion) 플로우** — 데이터 처리 방식 / 크루 리더 탈퇴 / 유예기간 / GDPR 문구 결정 필요. App Store 심사 필수.

### 🐞 구현 시도 실패 (없음)
모든 갭 1회 시도에 analyze + test 통과. 롤백 없음.

---

## 📋 오늘(2026-04-21) 권장 작업
1. **🟡 비밀번호 재설정 기능 기획 확정** — 이메일 템플릿 방향성 결정 후 구현 가능
2. **실기기(iPhone) 검증** — iOS 엣지 스와이프 뒤로가기 동작 확인 (DEVICE_TEST_QUEUE 1번)
3. **🔴 계정 탈퇴 플로우 정책 결정** — App Store 심사 요건이라 출시 전 블로커

### 옵션: 다음 야간 세션 영역 로테이션
인증/보안 🟢 전량 소진. 다음 야간은 **온보딩/UX** 또는 **입력 검증** 영역 오딧 권장.

---

## 🐞 열린 이슈 (기존, DEVICE_TEST_QUEUE 참조)
- Android GPS 거리 0km (실기기 필수)
- iOS 백그라운드 복귀 시 크래시 (실기기 + Xcode Capability 필요)
- BLE 심박수 실기기 검증 (시뮬레이터 미지원)
- iOS 엣지 스와이프/화면비/시간 측정 실기기 최종 확인

---

## 🚀 제약 준수 체크
- ✅ git push 없음
- ✅ 실기기 빌드 없음 (analyze + test만)
- ✅ Firebase rules/indexes 변경 없음
- ✅ 네이티브 파일(Info.plist, AndroidManifest, Xcode project) 수정 없음
- ✅ 7시간 타임박스 내 완료 (약 12분 소요)
- ✅ 커밋 상한(10개) 준수 (총 4개)
