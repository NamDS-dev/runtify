---
feature: iOS 엣지 스와이프 뒤로가기 (CupertinoPage 플랫폼 분기)
status: done
date: 2026-04-20 22:48
mode: 자동개발 (30분 타임박스)
started: 22:40
finished: 22:48 (8분 소요, 22분 여유로 조기 종료)
---

## 선택 이유
옵션 A/B/C 중 **A 선택** (가장 안전):
- A (iOS 스와이프 뒤로가기): 단일 파일, 디자인 변경 없음, 위험 최소 → ✅ 채택
- B (지도 컬러화): MapStyle JSON 생성 + 4개 지도 위젯 적용 → 30분 초과 우려로 제외
- C (미완 Phase): 실기기 빌드/Firebase 변경 수반 가능 → 제외

## 변경된 파일
- `lib/core/router/app_router.dart` — `_platformPage()` 헬퍼 추가 + 모든 `builder` → `pageBuilder` 전환
- `docs/FEATURE_PLAN.md` — iOS 엣지 스와이프 이슈 체크박스 `[ ]` → `[x]` (완료 메모 포함)
- `docs/STATUS.md` — 최근 완료 이력에 항목 추가
- `docs/handoffs/auto_2026-04-20-2240.md` — 본 핸드오프 파일

## 핵심 구현 결정사항
1. **플랫폼 분기 방식**: `!kIsWeb && Platform.isIOS` → `CupertinoPage`, 그 외 → `MaterialPage`
   - main.dart의 기존 패턴과 일치 (`dart:io show Platform` + `flutter/foundation show kIsWeb`)
2. **헬퍼 함수 추가 이유**: 17개 GoRoute 각각에 분기 로직을 반복하지 않도록 `_platformPage()` 1곳에 캡슐화
3. **ShellRoute `builder`는 유지**: 쉘은 페이지가 아닌 레이아웃이므로 변경 불필요
4. **CupertinoPage 선택 이유**: `MaterialPage` + `SwipeBackGesture` 수동 구현 대비 iOS 네이티브 제스처를 공식 지원 → 유지보수 부담 최소

## 검증 결과
- ✅ `flutter analyze --no-pub` — **No issues found!** (2.7s)
- ✅ `flutter test` — **All tests passed!** (widget_test.dart)
- ⬜ **실기기 검증은 사용자에게 위임** — 제약(실기기 빌드 금지)에 따라 의도적 스킵

## 커밋
```
5a5c313 iOS 엣지 스와이프 뒤로가기 지원 (CupertinoPage 플랫폼 분기)
```
- 스코프 내 파일(3개)만 선택적 스테이징, 기존 대기 중인 변경들은 건드리지 않음

## 제약 준수 체크
- ✅ git push 안 함 (로컬 커밋만)
- ✅ 실기기 빌드 안 함 (analyze + test까지만)
- ✅ Firebase 프로덕션 데이터 변경 없음
- ✅ 30분 타임박스 내 완료 (8분 소요)

## 사용자 돌아온 후 권장 액션
1. **실기기(iPhone) 테스트**: 임의 화면 진입 후 좌측 엣지에서 우측으로 스와이프 → 이전 화면으로 복귀되는지 확인
2. **애니메이션 차이 관찰**: iOS는 Cupertino 슬라이드, Android는 기존 Material 전환 유지됨 — 의도된 동작
3. **회귀 테스트**: 러닝 완료 결과(`/running/result`) 등 extra 파라미터가 있는 라우트도 정상 표시되는지 확인
4. **추가 작업(선택)**: 옵션 B(지도 컬러화)는 별도 세션에서 Figma 디자인부터 시작 권장

## 질문/미결 항목
없음. 이슈 없이 클린하게 종료.
