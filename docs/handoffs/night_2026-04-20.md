---
title: 야간 PM 활동 요약 (2026-04-20)
mode: /pm-night (30분 타임박스 세션 내 실행)
session_window: 22:40 ~ 22:52 (약 12분)
notion_upload: skipped (사용자 30분 타임박스 내 실행 + API 호출 시간 절약)
---

## 🌙 야간 PM 활동 요약 (2026-04-20 22:40 ~ 22:52)

### 📊 오딧 결과
- 영역: **인증/보안** (FEATURE_PLAN.md에 이미 4개 🟢 / 4개 🟡 / 1개 🔴 기록되어 있어 재오딧 생략)
- 발견된 갭: 🟢 4개 / 🟡 4개 / 🔴 1개 (기존)

### ✅ 자동 구현 완료 (1개)
- `78c7fcb` 인증 에러 맵핑 확장 + 계정 존재 힌트 차단
  - `lib/features/auth/data/datasources/auth_firebase_datasource.dart`
  - 주요 변경점:
    - `user-not-found` · `wrong-password` · `invalid-credential` · `invalid-login-credentials` → 동일 메시지로 통일 (계정 존재 힌트 차단 — OWASP 권고)
    - 9개 에러 코드 신규 매핑: `user-disabled`, `user-token-expired`, `requires-recent-login`, `network-request-failed`, `operation-not-allowed`, `account-exists-with-different-credential`, `credential-already-in-use`, `expired-action-code`, `invalid-action-code`
    - default 메시지에서 `e.message` 노출 제거 (내부 정보 누출 차단)
    - 섹션별 주석 추가 (로그인/회원가입/계정상태/레이트리밋/네트워크/인증방식/액션코드/기타)
  - 검증: `flutter analyze` 0 issues + `flutter test` pass

### 🟡 사용자 검토 필요 (기존, FEATURE_PLAN.md 참조)
- **[인증] 비밀번호 재설정 기능** — 재설정 이메일 템플릿 기본 vs 커스텀 결정 필요
- **[인증] 이메일 인증(Verification) 플로우** — 어느 기능까지 인증 전 허용할지 정책 결정 필요
- **[인증] 연속 로그인 실패 레이트 리밋** — Blaze 요금제 전환 여부 결정 필요
- **[인증] 세션 만료 및 토큰 갱신 처리** — 유휴 시간 정책 결정 필요

### 🔴 정책/보안 결정 필요 (기존, FEATURE_PLAN.md 참조)
- **[인증] 계정 탈퇴(Account Deletion) 플로우** — 데이터 처리 방식/크루 리더 탈퇴/유예기간/GDPR 문구 결정 필요. App Store 심사 필수.

### 🐞 구현 시도 실패 (없음)
해당 세션 중 재시도/롤백 없음.

### 🔄 남은 🟢 자동 구현 미착수 (시간 부족)
사용자가 원래 30분 타임박스를 걸어 추가 항목은 다음 야간 세션으로 이월:
- **[인증] 이메일 형식 검증 및 정규화** (45분 예상)
- **[인증] 로그아웃 후 민감 데이터 클리어 보강** (60분 예상)
- **[인증] 비밀번호 복잡도 규칙 적용** (90분 예상)

> 다음 세션(`/pm-night` 본격 실행 시) 상단 우선순위.

---

## 🎯 오늘 세션 진행 (30분 자동개발)

1. **iOS 엣지 스와이프 뒤로가기** (옵션 A) — `5a5c313`
   - `app_router.dart`에 `_platformPage()` 헬퍼 추가, iOS는 CupertinoPage로 분기
2. **핸드오프/상태 문서 업데이트** — `a8c9f73`
3. **인증 에러 맵핑 확장** (/pm-night 🟢 자동 구현) — `78c7fcb`

세션 총 로컬 커밋 3개, git push 없음.

---

## 📋 내일 권장 작업
1. **실기기(iPhone) 검증** — iOS 엣지 스와이프 뒤로가기 동작 확인
2. **🟢 이메일 형식 검증** — 가장 작은 다음 자동 구현 후보 (45분)
3. **🟡 비밀번호 재설정 기능 기획 확정** — 이메일 템플릿 방향성 결정

---

## 🐞 열린 이슈 (기존)
- Android GPS 위치 업데이트 이슈 (FEATURE_PLAN.md 기존 기록)
- iOS 백그라운드 복귀 시 크래시 (FEATURE_PLAN.md 기존 기록)
- 둘 다 실기기 필수 → 이번 세션 스코프 밖

---

## 🚀 제약 준수 체크
- ✅ git push 없음
- ✅ 실기기 빌드 없음 (analyze + test만)
- ✅ Firebase rules/indexes 변경 없음
- ✅ 네이티브 파일(Info.plist, AndroidManifest, Xcode project) 수정 없음
- ✅ 30분 타임박스 내 완료 (22:40 → 22:52, 12분 소요 / 23:10 마감 준수)
- ✅ 커밋 상한(10개) 준수 (총 3개)
